# Remaining work

## Web fetch

- Evaluate stemming-aware focused-section ranking against the benchmark corpus. Change ranking only when the benchmark improves without regressing heading and phrase selection, source-order preservation, or deterministic continuation offsets.
