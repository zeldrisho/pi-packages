{
  "name": "@zeldrisho/pi-packages",
  "private": true,
  "description": "Monorepo for independently published Pi extensions.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "vp check && vp test --coverage && vp run test:contracts && vp run pack:dry-run && vp run test:packages",
    "check": "vp check",
    "test": "vp test",
    "lint": "vp lint",
    "lint:fix": "vp lint --fix",
    "format": "vp fmt --write",
    "format:changelog": "vp exec node scripts/format-changelog.ts",
    "sync:web-modules": "vp exec node scripts/sync-web-modules.ts sync --from pi-web-fetch",
    "typecheck": "vp check --no-fmt --no-lint",
    "pack:dry-run": "for package in packages/*; do (cd \"$package\" && vp pm pack -- --dry-run) || exit; done",
    "test:contracts": "vp test tests/repository-contract.test.ts",
    "test:packages": "vp exec node tests/package-smoke-test.ts",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=24.10.0"
  },
  "packageManager": "pnpm@11.13.1"
}
