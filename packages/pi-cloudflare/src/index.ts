import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/**
 * Deprecated no-op. Move Cloudflare guidance into project context files or
 * task-specific documentation so it is loaded only when relevant.
 */
export default function cloudflare(_pi?: ExtensionAPI): void {}
