# @zeldrisho/pi-vite-plus

> **Deprecated:** This package is now a no-op. Toolchain commands should be documented in project context rather than appended to every system prompt.

Use the canonical [Vite+ CLI `AGENTS.md`](https://github.com/voidzero-dev/vite-plus/blob/main/packages/cli/AGENTS.md) as the source for project-scoped instructions; link to project documentation for details that are not needed on every task.

## Remove

```bash
pi remove npm:@zeldrisho/pi-vite-plus
```

For a project-local installation:

```bash
pi remove -l npm:@zeldrisho/pi-vite-plus
```

## License

[MIT](LICENSE)
