/** Default starting offset for content extraction (characters). */
export const FETCH_DEFAULT_OFFSET = 0;

/** Minimum allowed value for maxCharacters parameter. */
export const FETCH_MIN_MAX_CHARACTERS = 1_000;

/** Default maximum characters to return per fetch. */
export const FETCH_DEFAULT_MAX_CHARACTERS = 6_000;

/** Maximum allowed characters to return per fetch. */
export const FETCH_MAX_CHARACTERS = 30_000;

/** Maximum allowed offset for content extraction (characters). */
export const FETCH_MAX_OFFSET_CHARACTERS = 20_000_000;

/** Maximum allowed length for an optional focus query (characters). */
export const FETCH_MAX_QUERY_CHARACTERS = 400;

/** Maximum allowed length for input URLs (characters). */
export const FETCH_MAX_URL_CHARACTERS = 2_048;
